{
  "scripts": {
    "all": "concurrently --names \"backend,frontend\" --prefix-colors \"blue,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "cloudinary": "^2.9.0"
  },
  "devDependencies": {
    "@ffprobe-installer/ffprobe": "^2.1.2",
    "concurrently": "^9.0.0",
    "ffmpeg-static": "^5.3.0"
  }
}
